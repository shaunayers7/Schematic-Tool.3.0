# Schematic-Tool.3.0

This repository now includes a self-contained single-file HTML app:

- `/home/runner/work/Schematic-Tool.3.0/Schematic-Tool.3.0/index.html`

## Run

Open `index.html` directly in your browser.

## Customize

Edit the HTML, CSS, and JavaScript in `index.html` to change the app as needed.